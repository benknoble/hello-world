\documentclass{article}

% \usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{listings}
% \lstset{%
%     literate={π}{\pi}1
% }
\lstloadlanguages{Python}
% \def\pi{\ensuremath{\pi}}

\begin{document}
% front matter {{{
\title{Paper}
\author{D. Ben Knoble}
\maketitle
% front matter }}}

This is my text with \lstinline:inline code: in it.

\begin{equation}
    f(x) = x^2 + 2
\end{equation}

Here's some other code:

\begin{lstlisting}[language=Python]
def pi(i):
    return functools.reduce(lambda x, y: x + y, str(math.pi).split("."))[i]
\end{lstlisting}

And an included document:

\lstinputlisting[language=Python]{some.py}

\end{document}
